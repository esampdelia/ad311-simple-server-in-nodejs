# ad311-simple-server-in-nodejs

# Simple Web Server in Node.js

This project is a basic Node.js web server built for an AD311 assignment. The server demonstrates fundamental backend concepts including routing, serving static HTML files, and handling 404 responses.

---

## 📌 Project Overview

This Node.js server can:

- Serve an HTML file for the **home page** (`/`)
- Display a simple text response for the **about page** (`/about`)
- Return a **404 Not Found** message for any invalid routes

The project uses only built‑in Node modules (`http` and `fs`) and ES6 module syntax.

---

